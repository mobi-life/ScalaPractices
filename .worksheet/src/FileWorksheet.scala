
import io.Source._

object FileWorksheet {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(97); 
  val lines = fromFile("/src/matrix.txt").getLines();;System.out.println("""lines  : Iterator[String] = """ + $show(lines ))}
  
                                                  
}
